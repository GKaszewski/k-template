services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: sqlite:///data/app.db
      JWT_SECRET: change-me-in-production
      RUST_LOG: info
    volumes:
      - db_data:/data

volumes:
  db_data:
